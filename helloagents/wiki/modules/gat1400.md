# GAT1400

## 目的
将 `GA/T 1400.4-2017` 沉淀为本仓库 GAT1400 / VIID 开发、联调、代码审核和协议符合性检查的基线文档。

## 模块概述
- **职责:** 负责 GAT1400 注册、注销、保活、校时、本地订阅 HTTP 服务、结构化对象上报、订阅/注册状态通知
- **状态:** ✅已有实现，后续开发和审核必须回到标准基线检查
- **规范基线:** `GA/T 1400.4-2017`、`GA/T 1400.3-2017`、`GA/T 1399`、`RFC 2045`、`RFC 2616`、`RFC 2617`、`RFC 2818`
- **最后更新:** 2026-04-16

## 规范基线

### 标准适用范围
- `GA/T 1400.4-2017` 适用于公安视频图像信息应用系统的规划设计、软件开发和接口协议符合性测试。
- 本部分定义的是接口分类、协议结构、资源 URI、消息配对、交互流程和安全性。
- 具体对象字段和 Schema 不在本部分展开，统一回到 `GA/T 1400.3-2017`。
- 分析系统对象和能力模型统一回到 `GA/T 1399`。

### 接口分类

| 类别 | 交互双方 | 典型内容 |
|------|----------|----------|
| 采集接口 | 采集设备/采集系统/分析系统 -> 视图库 | 自动采集视频片段、图像、文件、人车物场景上传 |
| 数据服务接口 | 应用平台/其他系统/分析系统 <-> 视图库 | 特征查询、人工采集、布控告警、订阅通知 |
| 级联接口 | 上下级视图库之间 | 查询、布控、订阅、通知 |
| 分析接口 | 应用平台 <-> 分析系统 | 任务下发、任务状态、分析结果、分析能力 |

### 协议共性约束
- 所有接口交互信息都应抽象为 REST 资源，并由 URI 唯一标识。
- 资源按树状层级组织，当前标准定义两大顶级资源族：`/VIID/*` 和 `/VIAS/*`。
- 交互连接方式应支持 HTTP 长连接和短连接；`Protocol` 应支持 `HTTP`，宜支持 `HTTPS`。
- HTTP 请求头应增加 `User-Identify`，携带请求者系统用户 ID 等身份属性。
- 接口消息 `Content-Type` 应设为 `application/*+JSON`。
- `GET` 请求在 `2XX` 成功场景下返回资源对象；非 `2XX` 场景返回 `ResponseStatus`。
- `POST/GET/PUT/DELETE` 的语义分别对应 Create / Retrieve / Update / Delete。

### 公共时序要求
- 注册失败后，应在 `300s` 内随机延迟后重新注册。
- 注册成功后，`90s` 内如果没有业务交互，应主动发送保活。
- 上传和通知在一定延迟时间内未收到响应时，应判定失败并重传。
- 连续多次重传失败后，应视为链路问题，缓存未发送数据，重新注册成功后补传。

### 消息交互安全性
- 双方应执行身份认证，认证过程遵循 `RFC 2617` 摘要认证。
- 宜遵循 `RFC 2818`，即支持 HTTPS/TLS。
- 摘要认证的 `nonce` 超过 `3600s` 后应重置。

## 标准资源与当前工程关注面

### 视图库资源族 `/VIID/*`

| 资源类别 | 典型 URI | 标准要求 |
|----------|----------|----------|
| 系统资源 | `/VIID/System/Register` `/UnRegister` `/Keepalive` `/Time` | 注册、注销、保活、校时 |
| 基础资源 | `/VIID/APEs` `/APSs` `/Tollgates` `/Lanes` | 采集设备、采集系统、卡口、车道 |
| 视频图像资源 | `/VIID/VideoSlices` `/Images` `/Files` 及 `/<ID>/Info` `/<ID>/Data` | 元数据与二进制分离 |
| 结构化对象 | `/VIID/Persons` `/Faces` `/MotorVehicles` `/NonMotorVehicles` `/Things` `/Scenes` | 批量 CRUD 与单体 CRUD |
| 业务对象 | `/VIID/Cases` `/Dispositions` `/DispositionNotifications` | 案事件、布控、告警 |
| 订阅对象 | `/VIID/Subscribes` `/SubscribeNotifications` | 订阅、取消订阅、通知 |
| 分析规则对象 | `/VIID/AnalysisRules` `/VideoLabels` | 分析规则、视频图像标签 |

### 分析系统资源族 `/VIAS/*`

| 资源类别 | 典型 URI | 标准要求 |
|----------|----------|----------|
| 系统资源 | `/VIAS/System/Register` `/UnRegister` `/Keepalive` `/Time` | 分析系统注册、保活、校时 |
| 任务资源 | `/VIAS/Tasks` `/TaskControls` `/TaskStatuses` | 任务 CRUD、控制、状态 |
| 分析结果资源 | `/VIAS/Tasks/<ID>/VideoSlices` `/Images` `/VideoLabels` | 分析产出回传 |
| 能力资源 | `/VIAS/SystemCapability/*` | 分析、增强复原、检索能力查询 |

## 当前工程实现映射

| 标准能力 | 当前入口 | 现状 |
|----------|----------|------|
| 注册 / 注销 / 保活 / 校时 | `GAT1400ClientService::RegisterNow/UnregisterNow/SendKeepaliveNow/GetTime` | 已实现 |
| 本地注册显式开关 | `LocalConfigProvider(gat1400.ini::enable)` + `ProtocolManager::Start/RestartGatRegisterService` + `GAT1400ClientService::Start/Reload` | 已实现 `enabled`：关闭时不启动，运行中重载会先注销停服，重新启用后可从停止态重新拉起 |
| `User-Identify` 请求头 | `GAT1400ClientService::ExecuteRequest` | 已实现 |
| Digest 认证重试 | `http_auth.*` + `ExecuteRequest` | 已实现 |
| 对上请求地址模型 | `ProtocolExternalConfig.gat_register.scheme/base_path/request_timeout_ms` + `ExecuteRequest` | 已支持统一协议/地址前缀/超时配置，当前实际传输只实现 `HTTP`，`HTTPS` 仍未落地 |
| 订阅资源接收 | `HandleSubscribeHttp` | 已实现 `/VIID/Subscribes` 查询、新增、修改、删除，以及 `/VIID/Subscribes/<ID>` 取消订阅 |
| 结构化对象上报 | `ProtocolManager::NotifyGatFaces/NotifyGatMotorVehicles/NotifyGatNonMotorVehicles` -> `GAT1400ClientService::NotifyFaces/NotifyMotorVehicles/NotifyNonMotorVehicles` | 已实现给外部模块的 3 个明确入口；已注册直接上传，未注册时直接进入失败补传队列 |
| 结构化对象发送主链 | `PostFaces/PostPersons/PostMotorVehicles/PostNonMotorVehicles/PostThings/PostScenes` | 已实现批量上报 |
| 视频 / 图像 / 文件上报 | `PostVideoSlices/PostImages/PostFiles` | 已实现“先元数据后 Data”双阶段上传，发送时会先剥离元数据 JSON 中的 `Data`，再单独 `POST /<ID>/Data` |
| 告警 / 订阅通知 / 分析规则 / 标签 | `PostDispositions/PostDispositionNotifications/PostSubscribeNotifications/PostAnalysisRules/PostVideoLabels` | 已实现对上发送 |
| 上传失败缓存与补传 | `PendingUploadItem` + `PostJsonWithResponseList/PostJsonWithResponseStatus/PostBinaryData` + `ReplayPendingUploads` | 已实现最小文件队列、失败入队、重注册成功后回放和周期性补传 |
| Lower SDK 业务入口 | `LowerGAT1400SDK.cpp` -> `ProtocolManager::Instance()` -> `GAT1400ClientService` | 已实现运行时导出层，业务侧可直接调用 `LOWER_1400_POST_*`、注册观察者、透传自定义 HTTP 命令 |
| 采集设备资源访问 | `/VIID/APEs` | 标准定义为 `GET/PUT`，用于查询采集设备和动态口令更新 |
| 当前工程扩展调用 | `PostApes` | 保留为条件兼容扩展，只有 `gat_upload.enable_apes_post_compat=1` 时才允许 `POST /VIID/APEs` |
| `/VIAS/*` 分析系统服务面 | 当前仓库未见完整服务侧实现 | 当前 `ExecuteRequest` 会对 `/VIAS/*` 请求直接返回本地 `unsupported`，且不进入补传队列 |

### 现状提示
- 当前仓库的 GAT1400 重点是“下级 / 设备侧 lower client + 本地订阅接收”，不是完整的双向 VIID 资源服务器。
- `gat1400.ini` 当前新增 `enable` 持久化字段；`enabled=0` 时允许只保存停服态配置，运行中需显式调用 `RestartGatRegisterService()` 才会把 1400 生命周期真正切到停止态。
- 当前请求目标已经支持 `scheme + server_ip + server_port + base_path + request_timeout_ms` 统一建模，但实际传输仍只实现 `HTTP`，若平台要求 `HTTPS`，仍需补齐 TLS 链路。
- 标准要求的“失败缓存、重新注册、补传”已补入最小闭环：失败请求持久化到 `gat_upload.queue_dir`，注册成功和保活恢复后回放；但尚未形成独立 mock 联调测试。
- `PostBinaryData` 当前统一使用 `application/octet-stream` 承载 `/Data` 消息体；消息体内容仍按现工程约定直接放 Base64 文本，不再沿用 JSON content-type。
- `GAT1400ClientService` 当前只缓存 1400 自己的 `gat_register/gat_upload/gat_capture` 运行配置；涉及跨模块的设备 ID 回退时，现改为优先使用 `gat_register.device_id`，否则运行时通过 `ProtocolManager::GetGbRegisterConfig()` 现取 GB 注册设备 ID，不再长期缓存 `m_gb_register / m_device_id`。
- issue 43 最新收口后，GAT1400 keepalive demo 已全部删除；保活链路现在只负责注册状态维持与失败补传回放，不再夹带联调样例上报。
- 当前 `GAT1400Json` 对本轮联调样例补齐了 `FaceObject.ShotTime`、自动采集场景下非空时间字段透传，以及 `MotorVehicleObject.Speed/Direction/VehicleLength/PassTime`；`SubImage.Type` 现统一按两位数字序列化，如 `01/02/09/11/14`。
- 当前只保留 3 个结构化对象上报入口；若业务侧还需要图片 / 视频 / 文件上传，需继续使用现有 `PostImages/PostVideoSlices/PostFiles` 或 `LOWER_1400_POST_*` 链路自行编排。
- `POST /VIID/APEs` 不是当前标准基线里的常规 lower client 上报接口，现已收口为显式兼容开关 `gat_upload.enable_apes_post_compat`，默认关闭。
- `/VIAS/*` 在本阶段明确不支持，避免被透传命令误送到失败补传队列里反复重放。
- 当前业务侧接入建议优先走 `LowerGAT1400SDK` 导出层，而不是自行再管理一套 `GAT1400ClientService` 实例；导出层现直接复用 `ProtocolManager::Instance()` 对应的运行时服务。
- `LowerGAT1400SDK.cpp` 本地 `ProtocolManager` skeleton 当前只保留真实使用的非 `const GetGatClientService()` 声明，不再保留零调用的 `const` 访问壳层。
- 外部模块若只需上报人脸、机动车、非机动车结构化对象，建议优先走 `ProtocolManager::NotifyGatFaces/NotifyGatMotorVehicles/NotifyGatNonMotorVehicles()`；旧的 `NotifyGatAlarm()` 与 `GAT1400CaptureEvent/GAT1400CaptureControl` 抓拍桥接链路均已删除。

## 结构化对象上报使用说明

### 适用场景
- 外部模块只需要上报人脸、机动车或非机动车对象，不需要把图片 / 视频 / 文件绑成一条抓拍事件。
- 调用方已经自行组好了 `std::list<GAT_1400_Face>`、`std::list<GAT_1400_Motor>` 或 `std::list<GAT_1400_NonMotor>`。

### 推荐入口
- `ProtocolManager::NotifyGatFaces()`
- `ProtocolManager::NotifyGatMotorVehicles()`
- `ProtocolManager::NotifyGatNonMotorVehicles()`

### 当前发送策略
- 若 1400 当前已注册，协议模块会直接调用 `PostFaces/PostMotorVehicles/PostNonMotorVehicles` 发送。
- 若 1400 未注册或服务未就绪，不会走抓拍内存队列，而是直接把原始 JSON 持久化到 `gat_upload.queue_dir`，等待注册成功后由现有补传链路回放。

## 已移除的旧调用面

- `ProtocolManager::NotifyGatAlarm()`
- `GAT1400ClientService::NotifyCaptureEvent()`
- `App/Protocol/gat1400/GAT1400CaptureControl.*`
- 保活成功后自动触发的人脸 / 机动车 keepalive demo

## 开发清单
- 确认资源 URI、HTTP 方法和查询字符串与 `GA/T 1400.4-2017` 一致。
- 确认对象结构、字段约束和列表包装来自 `GA/T 1400.3-2017` 或 `GA/T 1399`，不要在 1400.4 中臆造字段。
- 所有 JSON 请求应带 `Content-Type: application/*+JSON`。
- 所有对上请求都应带 `User-Identify`。
- 对需要鉴权的链路，必须走摘要认证；若平台要求 HTTPS，需要显式支持。
- `/<ID>/Data` 资源的消息体是 `Base64` 编码二进制流，不能直接裸传文件字节。
- 注册失败、保活超时、通知失败、上传失败必须检查是否具备重试、缓存、重注册和补传闭环。
- `ResponseStatus` / `ResponseStatusList` 的 `Id`、`StatusCode`、`StatusString`、`LocalTime` 语义要与标准保持一致。

## 审核清单
- 这次改动是否改错了 URI、HTTP 方法或 `IDList` 的使用方式。
- 这次改动是否只改了请求体，却没有同步检查 `ResponseStatus` 处理逻辑。
- 这次改动是否遗漏了 `User-Identify`、Digest 鉴权或平台要求的 HTTPS。
- 这次改动是否把元数据资源和 `Data` 资源混在一起处理。
- 这次改动是否只测了成功路径，没有验证 401、超时、重试、补传、重注册。
- 这次改动是否误把 `GA/T 1400.4` 当作字段定义来源，而忽略了 `GA/T 1400.3` / `GA/T 1399`。

## 依赖
- 标准基线: `/home/jerry/silver/GAT1400.pdf`
- 代码入口: `rk_gb/App/Protocol/gat1400/GAT1400ClientService.*`
- 对象定义: `rk_gb/third_party/platform_sdk_port/CommonFile/CommonLib/CMS1400Struct.h`
- 观察者接口: `rk_gb/third_party/platform_sdk_port/CommonFile/CommonLib/LowerGAT1400Observer.h`
- JSON 封装: `rk_gb/third_party/platform_sdk_port/CommonLibSrc/GAT1400SDK/include/GAT1400Json.*`
- 认证实现: `rk_gb/App/Protocol/gat1400/http_auth.*`

## 变更历史
- 2026-04-16: 按 issue 45 为 `gat1400.ini` 与 `GatRegisterParam` 补齐 `enable/enabled` 开关；`ProtocolManager::Start()`、`RestartGatRegisterService()`、`GAT1400ClientService::Start()/Reload()` 现统一按该开关决定“不启动 / 注销停服 / 重新拉起”
- 2026-04-10: 按 issue 43 最新评论继续收口，删除 `ProtocolManager::NotifyGatAlarm()`、keepalive demo，以及 `GAT1400CaptureEvent/GAT1400CaptureControl` 抓拍桥接链路；当前对外只保留 `NotifyGatFaces()/NotifyGatMotorVehicles()/NotifyGatNonMotorVehicles()` 3 个结构化对象入口
- 2026-04-02: 按 issue 41 最新评论继续调整 keepalive demo：拆成人脸和机动车两个单次函数，分别读取 `/mnt/sdcard/test.jpeg` + `/mnt/sdcard/face.jpeg`、`/mnt/sdcard/test2.jpeg` + `/mnt/sdcard/motor.jpeg`，并去掉失败后的心跳重试，避免平台持续看到重复上报
- 2026-04-02: 根据平台样例重做 keepalive demo，保活成功后直接上报 `POST /VIID/Faces` 与 `POST /VIID/MotorVehicles`，默认读取 `/mnt/sdcard/test.jpeg` 并 mock 出人脸场景图/抓拍图及机动车大图/车牌图/特写图；同时补齐 `GAT1400Json` 的 `ShotTime/MarkTime/AppearTime/DisappearTime/Speed/Direction/VehicleLength/PassTime` 输出与两位数 `SubImage.Type` 序列化
- 2026-04-01: 将 keepalive demo 的平台通信改为 `POST /VIID/DispositionNotifications`，不再复用 `NotifyCaptureEvent()/PostFaces/PostImages`；当前以 `PersonObject + SubImage(Data)` 发送最小告警通知，默认读取 `/mnt/sdcard/test.jpeg` 并使用 `DISPDEMO_<DeviceID>` 作为 demo 布控标识
- 2026-04-01: 修正上一版 keepalive demo：去掉本地视频读取和 `video_slice_list` 上送，当前只保留通过 `NotifyCaptureEvent()` 发送的人脸 + 图片 demo
- 2026-04-01: 按 issue 41 最新评论调整 keepalive demo：去掉旧的 `SendKeepaliveDemoUploadOnce()` 直传实现，改为在保活成功后通过 `NotifyCaptureEvent()` 上送人脸检测事件，关联本地图片和视频，并限制总共触发 `5` 次
- 2026-04-01: 将 `GAT1400CaptureControl.h/.cpp` 从 `App/Media/` 迁移到 `App/Protocol/gat1400/`，明确抓拍桥接控制器归属 1400 协议模块
- 2026-04-01: 新增 `ProtocolManager::NotifyGatAlarm()` 作为 1400 业务通知入口；算法/编码侧可直接通知抓拍结果，1400 服务会在“已注册直接上传 / 未就绪或失败则入队”之间自动切换
- 2026-03-28: 收口 GAT1400 对 GB 注册派生设备 ID 的跨模块缓存，`SnapshotConfig/Register/Keepalive/Reload` 改为按需解析当前 device id，避免 `m_gb_register / m_device_id` 长期滞留在 1400 服务内
- 2026-04-01: 去掉 `GAT1400ClientService` 对 `GAT1400CaptureObserver` 的继承，抓拍桥接改为纯内存队列，由 1400 服务在注册成功和保活成功后显式 drain
- 2026-03-28: 新增 `GAT1400CaptureControl` 抓拍桥接层，支持编码/算法侧将人脸 / 机动车抓拍及关联图片 / 视频 / 文件入队，由 `GAT1400ClientService` 自动消费上传
- 2026-03-23: 基于 `GAT1400.pdf` 补齐协议基线、实现映射和开发 / 审核清单
