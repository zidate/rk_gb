[轻量迭代]

# 任务清单

- [√] 将零配置运行时使用的重定向 `server_id/server_ip/server_port` 固定到代码默认值，不再复用 `gb28181.ini` 中的标准注册参数
- [√] 保持 `gb28181.ini` 继续承载标准国标注册配置，避免运行时零配置参数回写污染标准配置
- [√] 同步更新知识库与变更记录，并迁移方案包到 `history/`
