diff --git a/sysdrv/tools/board/rk_ota/src/bootloader.cpp b/sysdrv/tools/board/rk_ota/src/bootloader.cpp
--- a/sysdrv/tools/board/rk_ota/src/bootloader.cpp
+++ b/sysdrv/tools/board/rk_ota/src/bootloader.cpp
@@ -907,2 +907,2 @@
-    if (tar_path == NULL) {
-        LOGE("'--tar_path=' must be set.\n");
+    if (tar_path == NULL && save_dir == NULL) {
+        LOGE("'--tar_path=' or '--save_dir=' must be set.\n");
@@ -920,2 +920,6 @@
-    if (access(savedir, F_OK))
-        mkdir(savedir, 0755);
+    if (access(savedir, F_OK)) {
+        if (tar_path == NULL || mkdir(savedir, 0755)) {
+            LOGE("Image directory %s is unavailable.\n", savedir);
+            return -1;
+        }
+    }
@@ -923,8 +923,11 @@
-    sprintf(unpack_tar_cmd, "tar -xf %s -C %s", tar_path, savedir);
-    if (system(unpack_tar_cmd)) {
-        LOGE("Unpack %s failed.\n", tar_path);
-        return -1;
-    }
-
-    LOGI("tar path = %s\n", tar_path);
+    if (tar_path != NULL) {
+        sprintf(unpack_tar_cmd, "tar -xf %s -C %s", tar_path, savedir);
+        if (system(unpack_tar_cmd)) {
+            LOGE("Unpack %s failed.\n", tar_path);
+            return -1;
+        }
+        LOGI("archive mode, tar path = %s\n", tar_path);
+    } else {
+        LOGI("prepared-image directory mode\n");
+    }
     LOGI("save path = %s\n", savedir);
diff --git a/sysdrv/tools/board/rk_ota/src/main.cpp b/sysdrv/tools/board/rk_ota/src/main.cpp
--- a/sysdrv/tools/board/rk_ota/src/main.cpp
+++ b/sysdrv/tools/board/rk_ota/src/main.cpp
@@ -29,2 +29,2 @@
-    LOGI("--tar_path=<path>                    Set upgrade firmware path.\n");
-    LOGI("--save_dir=<path>                    Set the path for saving the image.\n");
+    LOGI("--tar_path=<path>                    Set optional tar firmware path.\n");
+    LOGI("--save_dir=<path>                    Set image directory; without tar_path, read prepared images directly.\n");
